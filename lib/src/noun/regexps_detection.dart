part of grammer;

final singularPatterns = <RegExp>[
  RegExp(r'[bcdfgjkmruopquvxzw]$'),
  RegExp(r'ss$'),
  RegExp(r'sis$'),
  RegExp(r'thos$'),
  RegExp(r'pus$'),
  RegExp(r'tis$'),
  RegExp(r'^[a-z]{1}s$'),
  RegExp(r'us$'),
  RegExp(r'(n|r)is$'),
  RegExp(r'tmas$'),
  RegExp(r'[abefgjklpqywxyz]a$'),
  RegExp(r'[^i]ca$'),
  RegExp(r'[^n]da$'),
  RegExp(r'[^p]ha$'),
  RegExp(r'([^dvtlar])ia$'),
  RegExp(r'([^i]|(cteri|at|ul|at|asi|di|nd|ov|ri))a$'),
  RegExp(r'(z|h|g|b|r|l)eria$'),
  RegExp(r'[^ci]teria$'),
  RegExp(r'[^cb]ilia$'),
  RegExp(r'[^xi]lia$'),
  RegExp(r'[^e]ma$'),
  RegExp(r'[^aeu]na$'),
  RegExp(r'[^ciam][a-z]na$'),
  RegExp(r'[ifo]\w{2}na$'),
  RegExp(r'[^aip]ta$'),
  RegExp(r'[^rmpe][a-z]ta$'),
  RegExp(r'[^gart][a-z][a-z]ta$'),
  RegExp(r'(a)e$'),
  RegExp(r'people$'),
  RegExp(r'[^t][^t][^o][^i][^a]e$'),
  RegExp(r'[^i][^t]h$'),
  RegExp(r'[uacgzheimnot][a-z]i$'),
  RegExp(r'[^atvliopcbnmgfds][a-z]*i$'),
  RegExp(r'[a-z][^ertiopasl][a-z]+[a-z]i$'),
  RegExp(r'[^e]l$'),
  RegExp(r'[^e]n$'),
];

final pluralPatterns = [
  RegExp(r'men$'),
  RegExp(r'sortia$'),
  RegExp(r'^media$'),
  RegExp(r'^cilia$'),
  RegExp(r'abilia$'),
  RegExp(r'trivia$'),
  RegExp(r'[^jtma](chi*|yn|ol|am|i|umn|os|an|ermin|omb|ul|eol|cle|on|un)i$'),
  RegExp(r'(um|pa|ro)ni$'),
  RegExp(r'(uo|io)si$'),
  RegExp(r'onchi$'),
  RegExp(r'(o|u|a)li$'),
  RegExp(r'(e|i)i$'),
  RegExp(r'ies$'),
  RegExp(r'ves$'),
  RegExp(r'es$'),
  RegExp(r'[aouiey]ys$'),
  RegExp(r'oos$'),
  RegExp(r'ios$'),
  RegExp(r'[tfg]ee[sth]{1,2}e*$'),
  RegExp(r'[^aouies][aoui]s$'),
  RegExp(r'[ea]s$'),
  RegExp(r'[^aouie]s$'),
];
